import React from "react";
import styles from "./diaryClick.module.css";

const DiaryClick = (props) => (
  <div className={styles.clickCardModal}>
    <div className={styles.diary}></div>
  </div>
);

export default DiaryClick;
